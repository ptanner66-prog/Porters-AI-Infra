UPSTREAM AND DOWNSTREAM IMPACT ANALYSIS for:

$ARGUMENTS

This is an alias for /impact. Running the full upstream and downstream impact analysis.

See `.claude/commands/impact.md` for the full protocol.
